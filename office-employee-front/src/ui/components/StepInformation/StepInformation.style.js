import { styled } from "@material-ui/core/styles";

export const StepInformationContainer = styled("div")(({ theme }) => ({
  width: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
}));
